apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: registry-data
  namespace: claude-workers
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 20Gi
